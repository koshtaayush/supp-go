import * as React from "react";

function PostView() {
  // const answers = [];

  return (
    <div>
      {/* <div>Post title</div>
      <div>Post</div>

      <div>
        4 answers
        <div>
          {answers.map((v, index) => {
            return <div>Answer + {index}</div>;
          })}
        </div>
        </div> */}
      adsad
    </div>
  );
}

export default PostView;